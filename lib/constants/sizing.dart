import 'package:flutter/cupertino.dart';

const EdgeInsets padding_all = EdgeInsets.all(18.0);
const EdgeInsets padding_top = EdgeInsets.only(top: 18.0);
const EdgeInsets padding_left = EdgeInsets.only(left: 18.0);
const EdgeInsets padding_left_and_right =
    EdgeInsets.only(left: 18.0, right: 18);
